# Core deep learning / detection stack
# NOTE: On Kaggle, torch/torchvision are preinstalled with the correct CUDA build.
#       Locally, install the CUDA/CPU build of torch that matches your machine first
#       (see https://pytorch.org/get-started/locally/), then `pip install -r requirements.txt`.
ultralytics>=8.3.0
torch>=2.2.0
torchvision>=0.17.0

# Numerical / data
numpy>=1.24
pandas>=2.0
pyyaml>=6.0

# Image / video IO
opencv-python-headless>=4.9
pillow>=10.0

# Metrics & analysis
scikit-learn>=1.3
scipy>=1.11

# Visualization
matplotlib>=3.7
seaborn>=0.13

# Progress / utilities
tqdm>=4.66

# Demo app
streamlit>=1.32

# Testing
pytest>=8.0
